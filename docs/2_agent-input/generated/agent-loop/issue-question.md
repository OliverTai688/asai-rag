# Agent Loop Issue Questions

Last updated: 2026-06-24

## 使用者決策

- 2026-06-20 resolved: 使用者要求參考 NuvaClub automation，建立 ASAI 自動化。已定義 ASAI LV3 automation 目標為「架構、體驗、介面、容易操作簡約的沉浸式顧問系統成熟度」，不是自動宣稱 Level 3 public launch ready。
- 2026-06-20 resolved: ASAI LV3 目標流程定案為：新增客戶 -> 建立關係圖 -> 生成拜訪準備包 -> 檢視問題清單與推論依據 -> 從準備包建立劇場舞台 -> 劇場私聊/群聊/人物狀態更新 -> AI 訪談建立或補強客戶資料、準備包與劇場。
- 2026-06-20 resolved: 使用者要求「先不用 git push」。後續 ASAI LV3 automation 每輪仍需驗收、stage 與建立本地 commit，但不得 push；report/final 寫明 `push skipped by user instruction`。待使用者明確恢復 push 後再重新開啟。
- 2026-06-21 resolved: 使用者確認 git push 仍「先不要」，所以本地 commit policy 維持，push 不恢復。
- 2026-06-21 resolved: 使用者確認 NANDA / AgentFacts 暫時「先不要發布啟動」。Internal manifest、local adapter dry-run、registry readiness review 可繼續；external registry publication、public discovery endpoint、credential signing、cross-org agent access 仍不得啟用。
- 2026-06-21 resolved: 使用者批准 live AI provider proof。後續可實作 Route B director/character/feedback、Realtime voice 或其他 provider-backed proof，但每次 OpenAI/Anthropic success/error path 都必須寫 `AiUsageLog`，並記錄 usage/cost evidence；未設定 provider env 時仍以 guarded-disabled / manual-setting 記錄處理。
- 2026-06-21 resolved: 使用者要求 `BFF-305a` public lead capture 一起做，不再 deferred。Lead endpoint 必須具 rate limit、honeypot/spam protection、consent version、allowlisted DB persistence、abuse/failure proof；不得順手啟用 real email/notification/payment。
- 2026-06-21 resolved: 使用者要求 production/payment/email/notification 除環境設定外由 Codex 實作；需要 operator 手動設定的 env/provider console 項目集中寫入 `manual-setting.md`。Production destructive DB、remote deletion、refund/void 等高風險操作仍需逐項明確執行 approval。
- 2026-06-21 resolved: 使用者要求 AI Meeting / notes prototype 納入正式主線。後續可選 AMM slice 驗證並提交 AMM-owned docs/source；未驗收前仍不得把 prototype 當完成 proof 或 registry-ready。
- 2026-06-21 resolved: 使用者要求若剩餘證據搜集可由使用者自己跑一輪檢查完成，automation 不必糾結補齊所有殘餘 evidence；後續 loop 應在必要 proof 通過後，將可自行重跑的殘餘檢查以明確 command/URL/checklist 交給使用者自行查看。此偏好不得取代 source 行為、authz/privacy、provider `AiUsageLog`、DB safety 或 production approval 的必要 proof。
- 2026-06-22 open: Relationship confirmation card advisor-state 真正持久化需要 product/schema decision。目前已完成 transient owner-scoped validation boundary（只保留 `cardId/state/updatedAt/sourceReferenceIds/safeNoteSummary`，safe note redacts contact sentinels，且 `persistedToDatabase=false`）；若要 refresh/new-context 保存，需決定採 VisitPlan-owned JSON subdocument 或 dedicated table/migration，並補 migration/rollback、API/UI/DB/browser proof。未決前不得把準備包 local selection 宣稱成已持久化或已寫回 CRM fact。
- 2026-06-23 open update / L4 blocker analysis: Relationship confirmation persistence 已有 source contract 與 dry-run proof，但仍缺最小產品決策。Minimal operator answer needed（回覆其中一項即可）：Option A: `visit-plan-json-subdocument`，適合只需隨單一準備包 refresh/new-context reload、較少查詢與 audit history；Option B: `dedicated-relationship-confirmation-state-table`，適合需要 per-card write isolation、auditability、未來跨準備包查詢；Option C: `defer-relationship-confirmation-persistence`，明確延後 durable persistence，維持 `selectedOption=null`、`persistedToDatabase=false`，下一輪改做其他 source-backed LV3 bridge。未收到 A/B/C 前不得新增 DB persistence、migration、CRM fact write 或宣稱 advisor selection 已保存。自檢 proof：`pnpm visit:relationship-confirmation-persistence-blocker-qa`、`pnpm visit:relationship-confirmation-state-boundary-dry-run`。
- 2026-06-23 open update: Formal `RelationshipEdge` table migration 仍需 operator approval；REL-004a 已完成 no-schema `RelationshipEdgeDraft` shadow contract 與 `pnpm client:relationship-edge-shadow-qa` dry-run proof，但這不代表 Prisma schema、DB table、repository write path 或 persisted edge model 已完成。Minimal operator answer needed before REL-004：approve additive Prisma `RelationshipEdge` schema + migration/rollback note + development/staging DB proof，或 explicitly defer formal edge table and continue no-schema BFF/UI consumers. 未批准前不得執行 Prisma migration/db push 或宣稱 edge table ready。
- 2026-06-24 open update / fifth-loop review refinement: Relationship confirmation persistence 仍是產品/資料模型決策，不是 source/proof gap；目前 transient boundary 已可驗 `cardId/state/updatedAt/sourceReferenceIds/safeNoteSummary`，但 refresh/new-context durability 仍需 A/B/C 回覆。若使用者選 C/defer，下一輪應明確避開 persistence，改做 source-backed non-schema runtime bridge 或 cross-flow proof，不再重複 blocker analysis。
- 2026-06-24 open update / fifth-loop review refinement: Formal `RelationshipEdge` table 的 blocked scope 已縮小。REL-004a/b/c/d/e/f/g 已證明 no-schema shadow contract、BFF summary、previsit/theater handoff、theater build review、Route B session readback、next-turn/provider context、feedback review/provider grounding；剩餘只是不動 schema 無法提供 durable edge writes / backfill / graph edit persistence。Minimal operator answer remains: approve additive `RelationshipEdge` schema + migration/rollback + development/staging DB proof, or explicitly defer formal table and keep no-schema consumers. 未批准前不得執行 Prisma migration/db push 或宣稱 edge table ready。
- 2026-06-24 resolved/source update: REL-004e 已完成 no-schema RelationshipEdge shadow -> Route B session source grounding/readback；`relationshipEdgeShadow` 現在會從 `/theater/build` 帶進 `TheaterRouteBScene.sourceGrounding`，由 Route B session create/readback 保存，並在 `/theater/[sessionId]` 顯示 compact counts/warnings/boundaries panel。`pnpm theater:relationship-edge-shadow-session-source-qa` 可自行重跑檢查 create/read path、UI hook、AgentFacts manifest 與 no-provider/no-DB/no-write/no-draft-payload boundary。此結果仍不代表 formal `RelationshipEdge` Prisma schema/table/migration 已核可；上方 formal schema approval blocker 仍維持。
- 2026-06-23 resolved/source update: LV3-MEETING-PREP-001 已新增 `VisitMeetingRelationshipSignalDeck`，把 accepted AI Meeting quick note / summary / writeback candidate 安全摘要轉成拜訪準備包可用的關係確認 signal cards。`pnpm visit:meeting-relationship-signal-dry-run` 證明 confirmed/inference/unknown 分流、email/phone/policy/raw-payload redaction、no provider、no fake `AiUsageLog`、no DB persistence、no relationship graph/VisitPlan write、no direct confirmed CRM fact write。此結果不代表 UI 已顯示或 BFF 已持久化；若要進一步串 `/pre-visit/[planId]` 或 relationship graph writeback，需另做 owner-scoped UI/API proof。
- 2026-06-23 resolved/source update: LV3-MEETING-PREP-UI-001 已完成 owner-scoped BFF/UI consumption：新增 `GET /api/visits/[id]/meeting-relationship-signals`，由 server session + visitPlanId 找 latest `CLIENT_MEETING`、讀取 accepted quick-note memory、persisted summary 與 writeback preview，再在 `/pre-visit/[planId]` 右欄顯示「會議關係訊號」卡片。`pnpm visit:meeting-relationship-signal-bff-ui-qa`、`pnpm visit:meeting-relationship-signal-dry-run`、`pnpm ai:protocol-registry-qa` 證明 owner-scoped read、no browser-supplied session/person id、no provider/no fake `AiUsageLog`、no raw transcript/provider/contact/policy leakage、no relationship graph/VisitPlan write、no confirmed CRM fact write。此結果仍不代表 relationship confirmation advisor-state 已 refresh/new-context persistence；真正持久化仍需上方 product/schema decision。
- 2026-06-22 resolved: ITA-003n 已完成 Route B live next-turn provider-candidate route wiring；新增 owner-scoped `POST /api/theater/route-b/sessions/[sessionId]/next-turn/provider-candidate`，以 persisted `RouteBSessionSnapshot` + server-side next-turn draft + `RouteBProviderPromptContext` 呼叫 OpenAI JSON mode，success/error path 均先寫 THEATER/OpenAI `AiUsageLog`，guard paths no-provider/no-fake-usage/no-append。`/theater/[sessionId]` 只有 provider candidate 含 safe flags + `usageLogId` 時才啟用 append confirmation。`pnpm theater:route-b-next-turn-provider-route-qa`、`pnpm theater:route-b-provider-prompt-context-dry-run`、`pnpm ai:protocol-registry-qa`、`pnpm ai:bff-audit` 通過；若只剩瀏覽器截圖或成本型手動點擊 proof，使用者可在 dev server/provider key 可用時自行檢查，下一輪不應回到 docs-only evidence。
- 2026-06-22 resolved: ITA-005d 已完成 Route B severe red-line action workflow；`RouteBSevereRedLineActionWorkflow` 從既有 severe warning preview 同源產生 5 張 action cards，狀態包含 `WATCHING`、`EVIDENCE_NEEDED`、`NOT_APPLICABLE`、`ESCALATE`。`/theater/[sessionId]` 的「守門紅線」面板可切換 action state，並顯示 reason/evidence requirement、UI-local persistence boundary、no legal advice、no formal finding、no notification、no provider、no fake `AiUsageLog`、no confirmed CRM fact。`pnpm theater:route-b-red-line-action-workflow-dry-run`、`pnpm theater:route-b-severe-red-line-preview-dry-run`、`pnpm theater:route-b-provider-prompt-context-dry-run`、`pnpm ai:protocol-registry-qa`、`pnpm ai:bff-audit` 通過。此結果不代表正式法遵處置、DB persisted escalation、real notification 或 external NANDA registry publication 已完成；下一輪建議 `ITA-005e` owner-scoped safe action-state persistence boundary，避免退回純截圖或 docs-only proof。
- 2026-06-22 resolved: ITA-005e 已完成 Route B severe red-line action persistence boundary；`POST/GET /api/theater/route-b/sessions/[sessionId]/red-line-actions` 只允許 current member 讀寫 owner-scoped Route B session，`sceneState.redLineActionState` 只保存 `ruleId/state/advisorReasonCode/updatedAt`，並保留 no provider/no fake `AiUsageLog`、no notification、no legal advice、no formal finding、no raw private/provider payload、no confirmed CRM fact。`/theater/[sessionId]` 的「守門紅線」面板可讀取/保存 persisted state 並顯示 record count/latest updated。`pnpm theater:route-b-red-line-action-persistence-qa` 通過；`pnpm theater:route-b-persistence-qa` 已擴充為可由 operator 在 dev server/DB 可用時自行重跑的 live owner write/read、manager denial、DB scene_state 與 refresh/new-context proof。此結果不代表正式法遵審閱、real notification、live detection、feedback/prep/meeting consumption bridge 或 external registry publication 已完成。
- 2026-06-22 resolved: ITA-005f 已完成 persisted Route B severe red-line action state 的第一個 consumption bridge；`RouteBSessionSnapshot.scene.redLineActionState` 由 owner-scoped `sceneState.redLineActionState` 產生，`buildTheaterRouteBFeedbackReview()` 會把 persisted action summary 寫入 `TheaterRouteBFeedbackReview.redLineActionState`，並在 matching findings 附上 `actionContext`。`/theater/[sessionId]` 五視角回顧面板顯示 action-state source、升級審閱/需要佐證 counts 與 per-finding advisor action context。`pnpm theater:route-b-feedback-review-qa`、`pnpm ai:protocol-registry-qa` 通過，且證明 no-provider/no fake `AiUsageLog`、no notification、no legal advice/no formal finding、no raw private/provider payload、no confirmed CRM fact。此結果不代表 visit preparation package / AI meeting notes consumption、正式法遵審閱 workflow、real notification、live detection 或 external registry publication 已完成；下一輪因 cadence counter 將到 4，應先做 whole-product gap review，再決定是否接 prep/meeting consumption bridge。
- 2026-06-22 resolved: ITA/AMM-005h 已完成 visit preparation BFF/UI 自動載入 Route B red-line action context；`GET /api/visits/[id]/route-b-red-line-context` 從 owner-scoped VisitPlan 重新推導 `routeBSourcePacketId`，再查 current member/org/owner/client scoped Route B session 的 persisted `sceneState.feedbackReview`，`/pre-visit/[planId]` 顯示「劇場紅線回帶」面板且不要求顧問輸入 raw session/person ids。`pnpm visit:route-b-red-line-context-bff-qa`、`pnpm visit:route-b-red-line-context-dry-run`、`pnpm ai:protocol-registry-qa` 通過，證明 no-provider/no fake `AiUsageLog`、no notification、no legal/formal finding、no confirmed CRM fact write、no raw private/provider payload。此結果不代表 AI meeting notes consumer、正式法遵審閱 workflow、real notification、live detection 或 external registry publication 已完成；下一輪若做 notes consumer，需先審核/adopt 目前 dirty worktree 的 notes prototype 範圍。
- 2026-06-22 resolved: ITA/AMM-005i 已完成 AI meeting notes red-line context consumer；本輪先審核並隔離 untracked `/notes` local Zustand prototype，沒有把它當 committed baseline。正式落地面使用 `/pre-visit/[planId]/notes` + owner-scoped `MeetingWorkspace`：notes page 從 `GET /api/visits/[id]/route-b-red-line-context` 讀取安全子集合，MeetingWorkspace 顯示 `meeting-route-b-red-line-context` 面板並把提醒併入 manual-note draft。`pnpm meeting:route-b-red-line-context-qa`、`pnpm meeting:notes-compat-contract-dry-run`、`pnpm ai:protocol-registry-qa`、`pnpm ai:bff-audit` 通過，證明 no-provider/no fake `AiUsageLog`、no notification、no legal/formal finding、no confirmed CRM fact write、no raw theater session/person ids、no raw private/provider payload。此結果不代表 untracked `/notes` prototype 已正式採納、正式法遵審閱 workflow、real notification、live detection 或 external registry publication 已完成。
- 2026-06-24 resolved/source update: ITA/AMM-003p 已完成 AI meeting notes state proposal consumer；正式落地面仍是 `/pre-visit/[planId]/notes` + owner-scoped `MeetingWorkspace`，不採用 untracked `/notes` local prototype。Notes page 從 `GET /api/visits/[id]/route-b-state-proposal-context` 讀取 safe subset，MeetingWorkspace 顯示 `meeting-route-b-state-proposal-context` 面板，並把 requiresConfirmation=true 的 follow-up / evidence-needed / next-question reminders 併入 manual note draft。`pnpm meeting:route-b-state-proposal-context-qa`、`pnpm visit:route-b-state-proposal-context-qa`、`pnpm ai:protocol-registry-qa`、`pnpm ai:bff-audit`、`pnpm exec tsc --noEmit --pretty false`、`pnpm lint:changed` 通過；proof 明確 no provider/no fake AiUsageLog、no raw theater session/person/source packet id、no relationship graph/VisitPlan/confirmed CRM fact write。此結果仍不代表 formal `RelationshipEdge` table、relationship confirmation persistence、live provider proof 或 external registry publication 已完成。
- 2026-06-24 resolved/source update: ITA/AMM-003q 已完成 Route B state proposal -> AI Meeting writeback preview context bridge；`MeetingRouteBStateProposalWritebackBridge` 將 safe `VisitRouteBStateProposalContext` 轉成 `MEETING_WRITEBACK_PREVIEW_CONTEXT` cards，MeetingWorkspace 顯示 `meeting-route-b-state-proposal-writeback-bridge` panel，要求 persisted summary + advisor confirmation 後才可進一步 review writeback candidates。`pnpm meeting:route-b-state-proposal-writeback-bridge-qa`、`pnpm ai:protocol-registry-qa`、`pnpm exec tsc --noEmit --pretty false`、`pnpm lint:changed` 通過；proof 明確 no provider/no fake AiUsageLog、no raw theater session/person/source packet id、no relationship graph/VisitPlan/confirmed CRM fact write。此結果仍不代表 formal `RelationshipEdge` schema、relationship confirmation persistence、live provider proof 或 external registry publication 已完成。
- 2026-06-22 resolved: AMM-005j 已完成 global `/notes` local prototype quarantine；`/notes` 現在是 accepted-source entrypoint server component，宣告 `data-local-note-store="disabled"` 與 `data-accepted-notes-source="/pre-visit/[planId]/notes"`，只導向已驗收的 `/pre-visit`、postVisitNotes compatibility bridge、Route B red-line context consumer 與 CLIENT_MEETING workspace。`pnpm meeting:notes-hub-quarantine-qa` 驗證 `/notes` 不 import `@/components/notes`、`@/domains/note/store`、`useNoteStore`、`QuickNoteComposer`、`SEED_NOTES` 或 browser-local note storage，且 AgentFacts manifest/registry 保持 `asai.meeting.prototype` internal-only。此結果不代表 quick-note server persistence、CRM writeback、正式法遵審閱、real notification、live detection 或 external registry publication 已完成；若未來要採納 quick-note，需另做 server-owned BFF、scope checks、writeback boundary、schema/DB/browser proof。
- 2026-06-21 resolved: 本輪 scheduled fifth-loop whole-product review 已完成；cadence counter 重設為 0。Review 確認 BFF-401a 已關閉 checkout disabled/sandbox server-payload proof，BFF-402 仍是 release-hardening fallback；但依 LV3 核心沉浸流程與使用者「避免只改 docs 建 proof」偏好，下一輪正常 loop 建議 `AMM-001a formal meeting contract + no-provider summary skeleton proof`。此 slice 必須 source-backed：`MeetingSummary` / `MeetingCitation` / action item / participant pure types、transcript/manual-note -> summary skeleton helper、dry-run proof；不得把既有未追蹤 meeting/notes prototype 當已完成 baseline，除非下一輪明確選擇並完整驗收 AMM-owned scope。
- 2026-06-21 resolved: AMM-001b 已完成 AI Meeting additive persistence contract；Prisma schema 新增 `InterviewKind.CLIENT_MEETING`、`AiModule.MEETING`、`InterviewMeetingSummary`，並以 `pnpm meeting:persistence-contract-dry-run` 驗證 source turn / memory citation extraction、no provider、no DB write、no confirmed CRM fact write。此結果不代表 DB migration/db push 已執行，也不代表正式 meeting BFF route/UI 已完成；下一輪建議 `AMM-002a` member-scoped meeting sessions/turns BFF，若需寫 DB 先做 additive development migration execution proof。
- 2026-06-21 resolved: AMM-002a 已完成 member-scoped AI Meeting capture BFF；新增 `/api/ai/meeting/sessions`、`/[sessionId]`、`/[sessionId]/turns`，對目前 `.env` development Supabase target 執行 additive `pnpm exec prisma db push`，並以 `pnpm meeting:bff-qa` 驗證 unauth 401、member client/visitPlan scoped create 201、text/final voice transcript turn 201、stateless readback 200、manager 404、DB session/turn/memory rows、raw audio/provider-like payload blocked 且 `AiUsageLog` 150->150 unchanged。此結果不代表 summary provider route、meeting UI entrypoint、cross-meeting memory/chat 或 production migration 已完成。
- 2026-06-21 resolved: AMM-003a 已完成 deterministic/no-provider meeting summary persistence route；新增 `POST /api/ai/meeting/sessions/[sessionId]/summary`、`generateMeetingSummaryForMember()` 與 `pnpm meeting:summary-bff-qa`，驗證 unauth 401、空來源 409、owner summary create 201、raw provider-like payload 409 且不落 row、overwrite=false 409、overwrite=true 200、manager 404、DB `InterviewMeetingSummary` citations/sourceTurnIds/guardEvidence/provider-null proof、`AiUsageLog` 150->150 unchanged。此結果不代表 AMM-003b live provider JSON mode summary、success/error `AiUsageLog`、quota 429、meeting UI entrypoint、cross-meeting memory/chat 或 production migration 已完成。
- 2026-06-21 resolved: 本輪 scheduled fifth-loop whole-product review after AMM-003a 已完成；cadence counter 重設為 0。Review 確認 AMM backend 已有 accepted contract、DB/API capture、deterministic cited summary persistence，但顧問仍缺 no-raw-ID、可操作的正式 meeting workspace 入口。依使用者「避免單純只修改 docs 建立 proof」偏好，下一輪 normal loop 建議 `AMM-005a visible meeting workspace entrypoint`：優先從 `/pre-visit/[planId]` 接 accepted AMM BFF，證明 create/read `CLIENT_MEETING`、append turn、generate deterministic summary、refresh/new-context persistence、manager/foreign denial、desktop/mobile browser no overflow/console error 0、raw provider/audio/private sentinel blocked、no-provider `AiUsageLog` unchanged。既有未追蹤 meeting/notes prototype 只有在下一輪明確選擇、檢查、限縮、驗收 AMM-owned subset 時才可 stage；不得再以純文件或未驗收 prototype 當作 proof。
- 2026-06-21 resolved: AMM-005a 已完成 visible meeting workspace entrypoint；`/pre-visit/[planId]` 可點擊進入 `/pre-visit/[planId]/meeting`，workspace 以 BFF 自動建立/讀取 `CLIENT_MEETING`、append manual note/final transcript、POST deterministic summary，並新增 owner-scoped GET summary 支援 refresh/new-context persistence。`pnpm meeting:workspace-ui-qa` 證明 desktop/mobile console error 0、無水平 overflow、owner summary GET、manager session/summary 404、raw provider/audio sentinel 409 且不 echo、`AiUsageLog` 150->150 unchanged。此結果不代表 AMM-004 cross-meeting memory/chat、AMM-003b live provider summary、notes UI/domain prototype、dashboard/CRM global meeting entrypoints 或 AMM-006 writeback boundary 已完成；下一輪建議 `AMM-004a cross-meeting client memory/chat`。
- 2026-06-21 resolved: AMM-004a 已完成 deterministic/no-provider cross-meeting client memory-chat；新增 owner-scoped `POST /api/ai/meeting/sessions/[sessionId]/chat` 與 `POST /api/ai/clients/[clientId]/memory-chat`，grounding 來自目前會議記憶、同客戶跨 session memory、persisted meeting summary、least-disclosure CRM client/family/policy/report metadata，答案分 facts/inferences/unknowns 並附 citations。`pnpm meeting:memory-chat-qa` 證明 unauth 401、owner session/client chat 200、prior summary citation、CRM facts citation、manager session 404、manager client 403、raw provider sentinel 409 且不 echo、response 無 email/phone/policy id/raw transcript/provider payload、`AiUsageLog` 150->150 unchanged。此結果不代表 AMM-003b live provider summary、AMM-004b provider-backed memory-chat quota/error logging、AMM-006 writeback boundary 或 pgvector retrieval 已完成；下一輪建議 `AMM-006a meeting action-item / CRM writeback candidate boundary`，次順位 `AMM-003b`。
- 2026-06-21 resolved: AMM-006a 已完成 deterministic/no-provider meeting writeback boundary；新增 `/api/ai/meeting/sessions/[sessionId]/writebacks`、meeting writeback boundary/repository 與 `pnpm meeting:writeback-qa`，驗證 summary_required / summary missing 409、raw provider-like payload 409 且不 echo、confirmed decision -> CRM candidate、inference -> insight 且 CRM fact count = 0、action item / unknown -> follow-up task、高敏感 confirmed fact 缺 reason/riskAccepted blocked、manager 404、DB `InteractionEvent` audit evidence、`writesConfirmedCrmFact=false` 與 `AiUsageLog` 150->150 unchanged。此結果不代表 meeting workspace UI confirmation cards、dashboard/CRM global meeting entrypoints、AMM-003b live provider JSON summary 或 AMM-004b provider-backed memory-chat 已完成；下一輪建議 `AMM-003b provider JSON summary with success/error AiUsageLog proof`，避免退回 docs-only proof。
- 2026-06-21 resolved: AMM-003b 已完成 live provider JSON meeting summary；`POST /api/ai/meeting/sessions/[sessionId]/summary` 新增 `mode: "PROVIDER_JSON"`，由 stored owner-scoped `CLIENT_MEETING` turns 產生 cited `MeetingSummary`，success/error 均寫 MEETING/OpenAI `AiUsageLog`，summary row 持久化 `provider/model/usageLogId/generatedBy=provider-json`，並保留 deterministic fallback。`pnpm meeting:summary-provider-qa` 證明 401、raw provider-like payload 409/no row、provider-disabled 503/no fake usage、quota 429/no provider、forced provider error 502 + error log/no summary row、success 201 + citations/provider/model/usageLogId、overwrite=false no new log、overwrite=true same row update + another success log、manager 404/no log、DB `provider='OPENAI'` / `usage_log_id IS NOT NULL` / `guardEvidence.providerCallAttempted=true` / `storesRawProviderPayload=false` / `writesConfirmedCrmFact=false`。此結果不代表 provider-backed memory chat、meeting workspace writeback confirmation UI、dashboard/CRM global entrypoints、pgvector retrieval 或 external NANDA registry publication 已完成；cadence counter 現為 4，下一輪應先做 whole-product gap review。
- 2026-06-21 resolved: 本輪 scheduled fifth-loop whole-product review after AMM-003b 已完成；cadence counter 重設為 0。Review 確認 AMM foundation 已有 capture、workspace entrypoint、deterministic/provider cited summary、deterministic memory-chat 與 writeback API boundary，新的主缺口是 meeting workspace 尚未顯示/提交 writeback confirmation cards。下一輪 normal loop 建議 `AMM-006b meeting workspace writeback confirmation cards`：接 `GET/POST /api/ai/meeting/sessions/[sessionId]/writebacks`，顯示 confirmed/inference/action/unknown 候選、reason/riskAccepted、高敏感 blocked、created/blocked/skipped results，證明 inference 不成 CRM fact、confirmed 只成 CRM candidate audit、action/unknown 成 follow-up task、manager denied、raw private sentinel blocked、desktop/mobile browser safe、no-provider `AiUsageLog` unchanged。第二順位為 dashboard/CRM global meeting entrypoints；第三順位為 AMM-004b provider-backed memory-chat。
- 2026-06-21 resolved: AMM-006b 已完成 meeting workspace writeback confirmation cards；`src/components/meeting/meeting-workspace.tsx` 會在摘要就緒後讀取 `/writebacks` preview，顯示 confirmed/inference/action/unknown 候選、reason/riskAccepted、高敏感 blocked gate 與 created/blocked/skipped result。`pnpm meeting:workspace-writeback-ui-qa` 證明 summary-required UI、desktop/mobile console error 0、無水平 overflow、owner createdEvents、manager 404、raw provider/private sentinel 409/no echo、confirmed approved -> CRM candidate audit、inference CRM fact count = 0、action/unknown -> follow-up task、`writesConfirmedCrmFact=false`、no-provider `AiUsageLog` 153->153 unchanged。此結果不代表 dashboard/CRM global meeting entrypoints、AMM-004b provider-backed memory-chat、AMM-007 pgvector retrieval 或 external NANDA registry publication 已完成；下一輪建議 `AMM-005b dashboard + CRM global meeting entrypoints`。
- 2026-06-21 resolved: AMM-005b 已完成 dashboard + CRM global meeting entrypoints；dashboard「最近會議」可直接進 `/pre-visit/[planId]/meeting`，CRM client detail「AI 會議工作台」可進 `/crm/[clientId]/meeting` 並建立 client-scoped `CLIENT_MEETING`。`pnpm meeting:global-entrypoints-qa` 證明 dashboard/CRM desktop + CRM mobile console error 0、無水平 overflow、dashboard seeded visit direct entry、CRM direct meeting summary refresh/new-context persistence、DB `client_id` linked / `visitPlanId` null proof、manager read/create foreign meeting 404、raw provider payload 409/no echo、no-provider `AiUsageLog` 153->153 unchanged。此結果不代表 AMM-004b provider-backed memory-chat、AMM-008 cross-state proof pack、`/pre-visit/[planId]/notes` 與 `postVisitNotes` 相容升級、pgvector retrieval 或 external NANDA registry publication 已完成；下一輪建議 `AMM-004b provider-backed memory-chat`，避免回到 docs-only proof。
- 2026-06-21 resolved: AMM-004b 已完成 provider-backed meeting/client memory-chat；`POST /api/ai/meeting/sessions/[sessionId]/chat` 與 `POST /api/ai/clients/[clientId]/memory-chat` 支援 `mode: "PROVIDER_JSON"`，共用 owner-scoped grounding 與 least-disclosure citations，quota 429 / provider-disabled 503 不呼叫 provider也不寫假 log，forced provider error 502 與 session/client success 均寫 `MEETING` / `OPENAI` `AiUsageLog`。`pnpm meeting:memory-chat-provider-qa` 證明 unauth 401、raw provider-like payload 409/no echo、provider-disabled 503/no fake usage、quota 429/no provider、provider error + error log、session/client success + usageLogId、DB usage log trace/client linkage、manager 404/403 no log、response 無 email/phone/policy/raw transcript/provider payload。此結果不代表 AMM-008 cross-state proof pack、`/pre-visit/[planId]/notes` 與 `postVisitNotes` 相容升級、pgvector retrieval 或 external NANDA registry publication 已完成；下一輪建議 `AMM-008 cross-state AMM proof pack`。
- 2026-06-21 resolved: AMM-008 已完成 cross-state AMM proof pack，並避免 docs-only proof。新增 `pnpm meeting:cross-state-qa` 串接 member client create、relationship graph source、policy projection、visit plan、prior meeting memory、`/pre-visit/[planId]` AI 會議 workspace、provider summary quota/error/success `AiUsageLog`、desktop/mobile reload persistence、writeback preview/confirm boundary、provider session/client memory-chat、Realtime quota/provider/raw-audio rejection、org overview aggregate privacy、DB raw sentinel scan 與 usage delta。Proof 過程修正 memory-chat safety 誤掃 opaque id 的電話假陽性，並讓 client memory-chat 固定納入 CRM client profile anchor。此結果不代表 `/pre-visit/[planId]/notes` 與 `postVisitNotes` 相容升級、AMM-007 pgvector retrieval 或 external NANDA registry publication 已完成；cadence counter 現為 4，下一輪應先做 whole-product gap review。
- 2026-06-21 resolved: 本輪 scheduled fifth-loop whole-product gap review after AMM-008 已完成；cadence counter 重設為 0。Top gap 改為 `AMM-005c notes/postVisitNotes compatibility bridge`：`/pre-visit/[planId]/notes` 與 legacy `postVisitNotes` 仍未和正式 `CLIENT_MEETING` workspace、latest meeting summary、writeback confirmation、quick-capture boundary 合流。下一輪必須做 source-backed UI/API/DB/browser proof，不得只改 docs 或 staging 未驗收 notes prototype；第二順位為 REL-004 formal edge table schema（需 migration/rollback approval），第三順位為 AMM-007 pgvector retrieval（需 Supabase extension/operator path）。
- 2026-06-21 resolved by 2026-06-23 proof: AMM-005c source bridge 已完成；先前 local targeted proof `DEMO_QA_BASE_URL=http://localhost:3001 pnpm meeting:notes-compat-qa` 被 `.env` Supabase host DNS/DB 可達性阻擋。2026-06-23 以目前環境確認 DB `select 1` 通過，並用 `DEMO_QA_BASE_URL=http://localhost:3000 pnpm meeting:notes-compat-qa` 完成 owner success、manager denial、raw sentinel、AiUsageLog unchanged、desktop/mobile no overflow 與 refresh proof。
- 2026-06-21 open update: DB/DNS blocker 仍重現；本輪已新增 `pnpm meeting:notes-compat-contract-dry-run` 作 no-DB source/contract fallback，證明 latest-session 選擇規則與 notes bridge source contract。這不取代完整 Browser/API/DB proof；若 DB host 可解析，使用者可直接跑 `DEMO_QA_BASE_URL=http://localhost:<dev-port> pnpm meeting:notes-compat-qa` 查看剩餘 runtime evidence。若下一輪仍是同一 DNS/P1001，應升級成 L4 blocker analysis 或改選其他安全 source-backed slice，不再只補 evidence 文件。
- 2026-06-22 resolved by 2026-06-23 proof: AMM-005c DB/DNS blocker analysis 已可關閉；目前 direct host 仍無 IPv4 A record 但 IPv6 AAAA 可用，`pg` read 與完整 Browser/API/DB proof 已通過。注意：Next dev proof 應使用 `localhost`，或在 `next.config.js` 設定 allowed dev origin；用 `127.0.0.1` 會被 Next dev HMR/origin guard 擋住 hydration，不能當作產品 route 失敗。
- 2026-06-22 open update: ITA-003i Route B runtime orchestration integration 已完成 source/static/contract proof，但 DB-backed `pnpm theater:route-b-runtime-qa` 目前在連線 `.env` Supabase host `db.wwocdcicvpmbdmqvskzi.supabase.co` 時同樣回 `getaddrinfo ENOTFOUND`，且 demo auth 會經 Prisma 查 membership，無法在 DB DNS 壞掉時有效打 runtime API。這是殘餘 runtime evidence blocker，不是新產品決策；DB host 可解析後，使用者可直接跑 `pnpm theater:route-b-runtime-qa` 檢查 guarded-disabled DIRECTOR orchestration preview、private addressee、consecutive-speaker guard、no-provider/no fake `AiUsageLog`。下一輪不得只為這個 DNS blocker 重跑 proof，應改做 source-backed ITA-003j persisted turn consumption 或 ITA-004a feedback runtime contract。
- 2026-06-22 resolved: ITA-004a Route B 五視角 feedback source contract 已完成，不呼叫 provider、不要求 DB；`buildTheaterRouteBFeedbackContract()` 與 runtime `FEEDBACK` preview 現在提供五視角預設全選/可選子集、質化-only/no-score/no-ranking output contract、嚴重紅線 review、no-provider boundary 與 `writesConfirmedCrmFact=false` persistence envelope。`pnpm theater:route-b-feedback-dry-run`、`pnpm ai:protocol-registry-qa` 可驗證這段 source contract。這不代表 live feedback provider success/error `AiUsageLog`、feedback 文字持久化或 session end UI 已完成；下一輪若 DB/DNS 仍 blocked，優先做 provider logging contract 或其他 source-backed slice，不要只重跑 DNS proof。
- 2026-06-22 resolved: ITA-004b Route B feedback provider logging contract 已完成，不連外、不寫 DB；`runTheaterRouteBFeedbackProviderContract()` 以 injected provider / usage logger 證明 success path 必須寫 success usage record 後才回 feedback result，provider error path 必須寫 sanitized error usage record 後才回 error，且 provider input 只含 preview counts、五視角、紅線 labels 與 qualitative output rules。`pnpm theater:route-b-feedback-provider-dry-run` 可重跑此 proof。這不代表 live OpenAI/Anthropic feedback route、DB persisted feedback、session end UI 或 external registry ready 已完成；若後續只剩 DB-backed runtime evidence，DB host 恢復後可由使用者直接跑 `pnpm theater:route-b-runtime-qa` / relevant QA，不需 automation 反覆卡在搜證。
- 2026-06-22 resolved: 本輪 scheduled fifth-loop whole-product gap review after ITA-004b 已完成；cadence counter 重設為 0。Review 確認 Route B 現在的最高槓桿缺口是「session 消費 orchestration/feedback contract 產生下一個角色回合」，而不是再補文件或追 DB/DNS 截圖。下一輪 normal loop 建議 `ITA-003j Route B next-turn consumption / persisted role reply loop (guarded no-provider first)`：以 persisted `RouteBSessionSnapshot` + latest advisor turn + `buildTheaterRouteBOrchestrationPlan()` 產生 CHARACTER/NARRATOR next-turn draft；DB 可用時補 owner-scoped append/read proof，DB 仍 `ENOTFOUND` 時完成 pure contract/API dry-run，且使用者可在 DB 恢復後自行重跑 `pnpm theater:route-b-runtime-qa` 或相關 QA 檢查殘餘 runtime evidence。
- 2026-06-21 resolved: NAP-005 已完成 local-only adapter/export dry-run；11 個 internal-only AI manifests 可產生 NANDA AgentFacts-style JSON、MCP descriptor、A2A Agent Card 與 HTTPS metadata 四種本地草稿，並由 `pnpm ai:protocol-adapter-dry-run-qa` 證明 schema-valid、least-disclosure、versioned、revocable、無 private sentinel、無 provider/DB/external registry side effect。此結果不代表任何 external registry publication、signing、public discovery endpoint 或 cross-org agent access 已核可。
- 2026-06-20 resolved: RAS-001 已完成 role-aware sidebar navigation contract；member / org admin / platform / client portal 四套 manifest draft 與 `SPIN 舊版` legacy feature flag proof 已落地。此結果不代表 sidebar UI、workspace bootstrap、route guard 或跨角色 browser/auth 驗收已完成；下一步仍需 RAS-002 resolver/policy tests。
- 2026-06-20 resolved: 本輪 scheduled fifth-loop whole-product review 已完成；cadence counter 重設為 0。Top product gap 是 persisted Route B session 還缺 relationship-graph-centered 的可操作演練舞台；若 DB 可用，下一輪建議 `ITA-003f/S1 Route B relationship-graph stage map (no-provider)`。若 DB/DNS 仍 blocked，下一輪建議 `RAS-002` resolver/policy tests 作安全 fallback。
- 2026-06-20 resolved: RAS-002 已完成 server-side sidebar resolver / policy tests；`resolveSidebarSections()` 與 `pnpm nav:role-aware-resolver-qa` 覆蓋 collaborator、member、scoped manager、manager without unit、org admin、org owner、support、finance、super admin、app-session super admin、client viewer。此 proof 僅代表 deterministic resolver/policy boundary，不代表 sidebar renderer、workspace bootstrap、URL route guard 或 production browser auth 已完成；DB/DNS 若仍 blocked，下一輪建議 RAS-003。
- 2026-06-20 resolved: RAS-003a 已完成 workspace bootstrap navigation contract；`/api/workspace/bootstrap` 保留既有 user/org/membership/plan/quota/auth 欄位並新增 `navigation.sidebarSections`、`surfaceSwitches`、`defaultSurfaceRedirect`、`routeGuardAlignment`、`settingsRoutePolicy` 與 no-provider/no-db-write proof。`pnpm nav:workspace-bootstrap-qa` 覆蓋 member、owner、scoped manager、unscoped manager、legacy `AGENT` role、quota exhausted disabled state 與 org surface 不混 member routes。此 proof 仍不代表正式 URL guard/browser auth 已完成；下一段需 RAS-003b 對齊 `requireOrgAdminRoute`、`/team/settings` 與 hand-typed URL 行為。
- 2026-06-20 resolved: RAS-003b 已完成 route guard/settings split source proof；`requireOrgAdmin()` / `requireOrgAdminRoute()` 現在走 `canReadWorkspaceOrgAggregate()`，manager 必須有 managed unit scope，`/team/settings` 改用 `requireOrgSettingsRoute()`，`/api/org/settings` 改用 `requireOrgSettingsAdmin()`，owner/admin only。`pnpm nav:route-guard-qa` 覆蓋 member/scoped manager/unscoped manager/owner/admin 的 `/settings`、`/team`、`/team/settings`、`/api/org/settings`、`/super-admin`、`/client/*` policy denial；此 proof 是 deterministic source/fixture，不代表 RAS-005 跨角色 browser session matrix 已完成。若 DB 仍 blocked，下一輪建議 RAS-004 sidebar renderer 消費 bootstrap navigation。
- 2026-06-20 resolved: RAS-004a 已完成 sidebar renderer contract adapter；`buildWorkspaceSidebarRenderModel()` 將 RAS-003 bootstrap sections 轉成 renderer-friendly `primarySections` / `surfaceSwitches` / action union / `activeItemId`，`pnpm nav:sidebar-renderer-contract-qa` 覆蓋 member AI-first order、member/org assistant scope、surface switch、legacy SPIN flag、active state、manager downgrade、AI quota disabled、no-provider/no-db-write。此 proof 未接 `src/components/layout/sidebar.tsx` UI、未做 Browser QA，且本輪刻意不 staging 既有 sidebar `/notes` dirty diff；下一段仍需 RAS-004b UI wiring。
- 2026-06-20 resolved: 本輪 scheduled fifth-loop whole-product gap review after RAS-004a 已完成；cadence counter 重設為 0。Top product gap 仍是 persisted Route B session 缺 relationship-graph-centered 可操作舞台，因此 DB 可用時優先 `ITA-003f/S1 Route B relationship-graph stage map (no-provider)`；但本輪 DNS check `db.wwocdcicvpmbdmqvskzi.supabase.co` 仍無法解析，下一輪安全 fallback 改為 `RAS-004b sidebar UI wiring`，用 `buildWorkspaceSidebarRenderModel()` 接 `src/components/layout/sidebar.tsx`，並隔離既有 `/notes` dirty diff。AMM meeting module 與 quick-capture notes 是有價值的 emerging source gap，但目前多數文件仍未納入 committed/proven baseline，不應取代 stage map / RAS 主線 proof。
- 2026-06-20 resolved: RAS-004b 已完成 sidebar UI wiring；dashboard shell 現在用新的 `RoleAwareSidebar` 消費 server-built member/orgAdmin `buildWorkspaceSidebarRenderModel()`，保留 legacy `src/components/layout/sidebar.tsx` 與其既有 `/notes` dirty diff 不動。`pnpm nav:sidebar-ui-qa` 覆蓋 shell/layout wiring、member/org active item、member/org assistant scope、data-boundary/reduced-motion hooks、legacy sidebar 未混入，並保存 desktop/mobile fixture screenshots。此 proof 不代表 live cross-role Browser/session matrix、super admin/client portal surfaced UI 或 assistant scope payload 行為已完成；下一段仍需 RAS-005，且 DB-backed ITA/BFF proof 仍受 Supabase DNS 阻擋。
- 2026-06-20 resolved: 本輪 scheduled fifth-loop whole-product gap review after PIM-011 已完成；cadence counter 重設為 0。五視框確認 product-level primary 仍是 `ITA-003f/S1 Route B relationship-graph stage map (no-provider)`，但 Supabase DB host `db.wwocdcicvpmbdmqvskzi.supabase.co` 仍回 `No answer`，所以 DB 未恢復前下一輪可執行 fallback 是 `PIM-011 post-visit quick-capture -> Park memory bridge`。此 fallback 必須 no-provider/no-schema、證明 `AiUsageLog` unchanged、member owner success、manager/foreign denied、high-sensitive gate、no private sentinel、refresh/new-context memory readback；不得 stage 既有 untracked AI meeting/notes prototype，除非下一輪明確選擇並驗證該整包 slice。
- 2026-06-20 resolved: PIM-011a 已完成 quick-capture -> Park memory bridge source contract 與 `pnpm interview:quick-capture-bridge-dry-run`；此 proof 只證明 no-provider/no-schema domain helper：serverScope 優先於 clientProvidedScope、confirmed/inference/unknown 分流、inference 不變 CRM candidate、unknown 轉 narrator question、state proposal 不寫 confirmed CRM fact、高敏感 gate、secret/raw payload block、`providerCallAttempted=false` / `aiUsageLogRequired=false`。它不代表正式 BFF/API persistence、owner 200、manager/foreign denial、refresh/new-context DB readback 或 UI browser proof 已完成；Supabase DB DNS blocker 仍適用。
- 2026-06-21 resolved: PIM-011b 已完成 quick-capture -> Park memory BFF/API proof；新增 `/api/ai/interview/quick-captures`、server-scoped `createPersistentQuickCaptureBridge()`、`pnpm interview:quick-capture-bff-qa` 與 AI route audit manifest。Proof 覆蓋 unauth 401、member owner 201、server session scope 覆蓋 rogue `clientProvidedScope`、high-sensitive client 缺 approval 409 且不落 session/turn/memory、snapshot refresh/new-context readback、turn anchor 不 echo raw note、manager read/write 404、unknown narrator question + state proposal、inference state proposal 且 CRM candidate = 0、secret/raw payload 409 且不落地、response no private sentinel、`AiUsageLog` 147->147。尚未完成的是正式 `/pre-visit/[id]/notes`、meeting workspace 或全站 quick-capture selector UI/browser proof。
- 2026-06-21 resolved: 本輪 scheduled fifth-loop whole-product gap review 已完成；cadence counter 重設為 0。Primary slice 仍是 `ITA-003f/S1 Route B relationship-graph stage map (no-provider)`，但 2026-06-21 再次 `nslookup db.wwocdcicvpmbdmqvskzi.supabase.co` 仍回 `No answer`，所以 DB 未恢復前不得宣稱 Route B stage map、BFF-103d 或 PIM-011 BFF/API DB-backed proof 完成。若 DB 持續 blocked，下一輪 no-DB fallback 改為 `LCH-009 production build font blocker`：讀 Next 16 bundled docs，修復 `next/font/google` / Turbopack Google font path blocker，跑 `pnpm build`，並保持 unrelated AMM/notes prototype files 不 staging。
- PIM-009 已完成 cross-mode QA 與 rollback note；尚未啟用 production recording 或 live Realtime provider proof。
- 2026-06-20 resolved: BFF-104a 已完成 Visit / Pre-visit server-owned workspace；`/pre-visit` list/detail/notes/create/update 改走 member-scoped BFF，`/api/ai/visit` 仍只負責 provider generation，deterministic save/update 另走 `/api/visits`。Proof 不呼叫 provider，僅做 demo `VisitPlan` 非破壞性新增/更新 evidence。
- 2026-06-20 resolved: TDF-004a 已完成 persisted visit package -> theater build 高敏感 gate；準備包來源審查顯示 known/inference/unknown，缺 reason/riskAccepted 會 blocked，approval 寫 `InteractionEvent` audit。Proof 不呼叫 provider；仍待 TDF-004b 補 `/theater` client selector 與越權 403 proof。
- 2026-06-20 resolved: TDF-004b 已完成 `/theater` client selector + owner-scoped client-data build review；`/api/theater/client-builds/[clientId]` 對 manager/member 越權回 403，高敏感 client 直建場 blocked，browser proof 顯示 known/inference/unknown review。Proof 不呼叫 provider。
- 2026-06-20 resolved: BFF-103a 已完成關係圖來源審查 DTO 與 `/api/clients/[id]/relationship-graph`；每個人物節點含職位、年收入、狀態、關係脈絡、fact/inference/unknown 與 source references，CRM 關係圖頁可見準備包/劇場 readiness。Proof 不呼叫 provider；仍待後續補 family edit/delete remote-confirmed write path。
- 2026-06-20 resolved: BFF-103b 已完成 family member remote-confirmed write path；`PATCH/DELETE /api/clients/[id]/family-members/[memberId]` 支援關係人更新、re-parent 與刪除，會阻擋 self-parent/cycle，刪父節點時子節點接回 root，CRM 關係人清單刪除改走 remote delete。Proof 不呼叫 provider；BFF-103 仍待 client archive/update、related-list BFF 與主客戶 `parentMemberId` 持久化。
- 2026-06-20 resolved: BFF-103c 已完成 CRM client lifecycle source-truth；`PATCH /api/clients/[id]` owner-scoped update proof 串到 relationship graph，新增 `DELETE /api/clients/[id]` soft archive 並保留 `complianceChecklist`、`sensitivityLevel`、`kycStatus`。Proof 不呼叫 provider，`AiUsageLog` count 不變；DB 顯示 `clients.status=ARCHIVED` 且 `compliance_checklists` 仍 attached；archived client 在 detail、relationship graph 與 `/crm` list refresh/new context 中消失。BFF-103 仍待 policy/timeline/report/gap-analysis related-list BFF DTO。
- 2026-06-20 resolved: BFF-001 已完成全站資料來源盤點與 responsibility matrix；新增 `AUD-006_full-site-bff-data-source-inventory-v1.0.md` 與 `pnpm bff:inventory-qa`，點名 DB/BFF ready、mixed、local/Zustand truth、static/mock 與 UI-only browser state。此輪未呼叫 provider、未做 DB/Prisma 操作、未重寫 route；下一張最低未完成 BFF 卡為 BFF-002 Shared API foundation。
- 2026-06-20 resolved: BFF-002 已完成 shared API foundation；新增 `src/lib/api/errors.ts`、`response.ts`、`validation.ts`、`sanitize.ts`，接入 member settings、share token/event、client portal response，並以 `pnpm bff:foundation-qa`、`pnpm bff:foundation-api-qa`、`pnpm share:token-qa`、`pnpm client-portal:qa` 證明 no-store/request-id/sanitized validation error 與 unsafe payload 不落地。此輪未呼叫 provider、未改 Prisma schema；API proof 只新增可辨識 demo/test share/client portal event evidence。
- 2026-06-20 resolved: BFF-105 已完成 Reports / share action BFF；`/reports`、`/reports/[reportId]` 與 CRM report subpage 改走 member-scoped `/api/reports` list/detail/create/update/share contract，public share 維持 `/api/share/[token]` client-safe DTO。`pnpm bff:reports-qa` 證明 invalid section 400、foreign manager 404、success share 寫 sanitized `ShareEvent`、public share 不回 internal/performance section、browser refresh/new context persistence。此輪未呼叫 provider、未改 Prisma schema；DB proof 只新增可辨識 demo/test `Report`、`ReportShare`、`ShareEvent` evidence。
- 2026-06-20 resolved: 本輪 scheduled fifth-loop whole-product review 已完成；cadence counter 重設為 0，下一輪 normal implementation/proof 建議 `BFF-106 Issues BFF`。原因：reports/share 已 server-owned，`/issues` 仍是 `MOCK_ISSUES`，且 issue fact/inference/unknown/next-action contract 會影響 dashboard decision center、previsit question rationale 與 org coaching aggregate。
- 2026-06-20 resolved: BFF-106 已完成 Issues BFF；`/issues` 從 `MOCK_ISSUES` 改為 server page + client component，初始資料來自 member-scoped `GET /api/issues` / repository，status/action/feedback/assignment 更新走 `PATCH /api/issues/[id]` 並寫 `AuditLog(resourceType=ISSUE)`。DTO 明確分 facts / inferences / unknowns、source references、internal readiness（固定 `clientFacingVisible=false`）與 advisor next action。`pnpm bff:issues-qa` 證明 unauth 401、member success、manager foreign 404、empty query、invalid patch 400、status/action audit、refresh persistence、desktop/mobile no overflow、no raw private sentinel；未呼叫 provider、未改 Prisma schema。下一輪建議 `BFF-101 Member dashboard BFF`，讓首頁決策中心消費 server-owned issue/task signal。
- 2026-06-20 resolved: BFF-101 已完成 Member dashboard BFF；新增 `GET /api/member/dashboard` 與 `MemberDashboardDto`，由 current member server scope 聚合 clients / visits / reports / issues / `AiUsageLog`，回今日主線、下一步 CTA、KPI、task queue、recent activity、agenda、AI quota summary 與 reasoning facts/inferences/unknowns。`/dashboard` 改為 server page + thin client component，不再以 `clientService.getDashboardStats()`、`eventService.getLatestEvents()` 或 `useSessionStore()` 作 business truth。`pnpm bff:dashboard-qa` 證明 unauth 401、member 200/no-store/request-id、manager own-dashboard 隔離、seeded issue task reload、desktop/mobile no overflow、no raw private sentinel；未呼叫 provider、未改 Prisma schema。下一輪建議 `BFF-301 Team/org aggregate BFF`。
- 2026-06-20 resolved: BFF-301 已完成 Team/org aggregate BFF；新增 `OrgTeamDashboardDto`、org aggregate repository 與 shared org API error helper，`/api/org/overview`、`/api/org/coaching`、`/api/org/ai-usage` 改為 protocol/session/private response shell，`/team` 改由 server page 讀 `getOrgTeamDashboardForSession()` 並移除 `MOCK_TEAM_MEMBERS`。`pnpm bff:org-aggregate-qa` 證明 unauth 401、member 403、manager 200/no-store/request-id、database/org-aggregate visibility、`/team` desktop/mobile no overflow、no client detail/report body/transcript/policy/private memory sentinel；既有 `demo:manager-aggregate-qa`、`demo:org-coaching-ai-usage-qa`、`demo:org-members-qa` 仍通過。未呼叫 provider、未改 Prisma schema。下一輪建議 `BFF-201 AI BFF audit gate` 或 `BFF-203 SPIN hardening`，依是否先重跑 AI audit 基線決定。
- 2026-06-20 resolved: BFF-201 已完成 AI BFF audit gate；新增 `pnpm ai:bff-audit` alias，`scripts/ai-usage-route-audit.mjs` 現在自動掃描 `src/app/api/ai/**/route.ts` 與 `src/app/api/rag/**/route.ts`，並以 manifest gate 證明 22/22 route covered、13 條 provider-ready route 有 session/quota/input/success-error `AiUsageLog` evidence、8 條 deterministic interview BFF route 有 session/input/no-provider proof、`/api/rag` 為 guarded-disabled no-provider posture。此輪未呼叫 provider、未改 Prisma schema；cadence counter 已到 4，下一輪應進 scheduled fifth-loop whole-product gap review。
- 2026-06-20 resolved: 本輪 scheduled fifth-loop whole-product gap review 已完成；cadence counter 重設為 0。Top gap 改為 `REL-001/REL-002 no-schema relationship graph build/persistence repair`，因 source audit 顯示 Prisma `Client` model 無 `parentMemberId` 但 domain/DTO/repository/UI 仍讀寫該欄位、`RelationshipMap.tsx` 長輩 edge 會孤立、`AddRelationshipDialog.tsx` parent mode 仍 local-only。下一輪建議先不動 schema、不呼叫 provider，改用既有 `FamilyMember.parentMemberId` + BFF write path 做 parent/elder relation refresh/new-context proof。
- 2026-06-20 resolved: REL-001/REL-002 已完成 no-schema relationship graph build/persistence repair；runtime 不再讀寫 `Client.parentMemberId`，root-connected 長輩會畫成長輩→主客戶 edge，`AddRelationshipDialog` parent mode 改走 `createFamilyMemberRemote` + `updateFamilyMemberRemote` re-parent，local family write helpers 標 dev-only。Proof：`pnpm client:relationship-graph-write-qa` 覆蓋 API 401/400/200/404、root elder reload、browser toolbar 新增父節點後 reload 持久化、desktop/mobile no overflow；`pnpm client:relationship-graph-qa` 仍通過來源審查與 seeded email/phone sentinel。下一輪建議 `REL-003 relationship graph BFF edge convergence`。
- 2026-06-20 resolved: REL-003 已完成 relationship graph BFF edge convergence；`ClientRelationshipGraphReview` 新增 typed edge list 與 `sourceSummary.edgeCount`，由 `FamilyMember.parentMemberId` + `relation` 推導 `PARENT_OF` / `SPOUSE_OF` / `SIBLING_OF` / `SOCIAL_TIE`，`RelationshipMap` 改吃同一份 review builder 的 nodes/edges，配偶/手足同 rank、社會關係不參與階層 rank。Proof：`pnpm client:relationship-graph-qa` 覆蓋 edge types、edge factStatus、node/edge count、email/phone sentinel、desktop/mobile no overflow；`pnpm client:relationship-graph-write-qa` 仍通過 parent create/reload/delete proof。下一輪建議 `REL-005 relationship graph layout/interaction polish`；REL-004 edge table schema 仍需 migration/rollback approval。
- 2026-06-20 resolved: REL-005 已完成 relationship graph layout/interaction polish；`RelationshipMap` 節點可 keyboard focus，toolbar 在 focus/selected 時顯示，新增父/子節點 action 具 `aria-label`、native `title` tooltip、focus ring 與 reduced-motion-friendly transition；配偶/手足/社會關係同 rank spacing deterministic。新增 `pnpm client:relationship-graph-polish-qa` 覆蓋空關係、單親、雙親＋配偶＋手足＋社會關係，並驗證 API edge types、準備包 question handoff、劇場 readiness、desktop/mobile/reduced-motion/dark browser proof、toolbar aria/title/focus、no overflow、console error 0、email/phone sentinel 0。未改 Prisma schema、不呼叫 provider；REL 剩餘 blocker 為 REL-004 edge table schema 需 migration/rollback approval。
- 2026-06-20 resolved: ITA-003a 已完成 Route B additive schema / typed persistence adapter proof；新增 `TheaterCharacter`、Route B character role / visibility scope enum、`TheaterSession` Route B scene/source/state metadata、`TheaterTurn` speaker/addressee/visibility/director/state patch 欄位與 `pnpm theater:route-b-schema-dry-run`。本輪只跑 local `prisma validate/generate`，未 db push、未 provider call；production/runtime 仍需正式 migration approval、DB rollback QA、director/character/feedback provider route 與 `AiUsageLog` success/error proof。
- 2026-06-20 resolved: ITA-003b 已完成 Route B runtime guarded-disabled BFF proof；新增 `POST /api/theater/route-b/runtime` 與 `pnpm theater:route-b-runtime-qa`。`SESSION_DRAFT` 只產生 deterministic draft summary，`DIRECTOR` / `CHARACTER` / `FEEDBACK` 在 provider flag 未開時回 503 guarded-disabled，證明 no provider call、no fake `AiUsageLog`、private visibility、state patch 不寫 confirmed CRM fact、response 無 raw private sentinel。未做 DB push；production migration 與 provider success/error proof 仍需後續。
- 2026-06-20 resolved: ITA-003c 已完成 Route B persisted session read/write proof；對目前 `.env` development Supabase Postgres target 執行 `pnpm exec prisma db push`（無 `--accept-data-loss`）成功，新增 `POST/GET /api/theater/route-b/sessions`、member-scoped BFF repository 與 `pnpm theater:route-b-persistence-qa`。Proof：create 201、owner read 200、manager read 404、DB rows 落地、response 無 raw private sentinel、`AiUsageLog` count before/after 不變。此 proof 不呼叫 provider；provider success/error `AiUsageLog` 與 session UI 仍待後續。
- 2026-06-20 resolved: ITA-003d 已完成 Route B persisted session UI/read surface；`/theater/build` 可從 typed handoff 建立 DB-backed Route B session 並導向 `/theater/[sessionId]`，session 頁可讀 persisted DTO 並顯示多角色舞台、角色卡、群聊/私聊 lane、導演開場、關係/旁白補問、visibility proof 與 provider guarded-disabled。`pnpm theater:route-b-session-ui-qa` 通過 desktop/mobile browser proof、manager 404、no private sentinel、provider action disabled、`AiUsageLog` count before/after 不變；provider success/error `AiUsageLog`、真正群/私聊回合寫入、人物狀態更新互動與五視角 feedback runtime 仍待後續。
- 2026-06-20 resolved: ITA-003e 已完成 Route B persisted interaction write shell；`POST /api/theater/route-b/sessions/[sessionId]/turns` 支援 owner-scoped 顧問 `AGENT` turn 寫入、群聊/私聊 lane、私聊 addressee、待確認人物狀態 proposal persistence。Proof：unauth 401、member group/private 201、invalid private 400、manager 404、DB group/private/state patch proof、browser submit proof、response/page 無 raw private sentinel、`AiUsageLog` THEATER count before/after 不變；未呼叫 provider。Route B AI 角色回覆 orchestration、director/character/feedback provider success/error `AiUsageLog` 與五視角 feedback runtime 仍待後續。
- 2026-06-21 resolved: ITA-003g 已完成 Route B director/character/feedback guarded runtime preflight；`POST /api/theater/route-b/runtime` 在 provider gate 前回傳 source-aligned `runtimeInputPreview`，含 `asai.theater.route_b`、action id、input contract、missing fields、visibility summary、provider boundary 與 success/error `AiUsageLog` plan。Proof：unauth 401、invalid handoff 400、draft 200、director/character/feedback guarded-disabled、missing director utterance 400、unknown character 400、private/provider sentinel 0、THEATER `AiUsageLog` 10->10；未呼叫 provider。Live provider usage 仍需 operator 明確批准，且 success/error path 必須寫 `AiUsageLog`。
- 2026-06-20 resolved: LV3 previsit 準備包介面已依外部 pre-call planning 研究重整為「專案情境 → 核心問題清單 → 決策地圖 → 完整 SPIN 問題庫 → 推論依據 → 劇場建場」；`/pre-visit/[planId]` first screen 可見職位/年薪/狀態/關係脈絡、核心問題與已知/推論/待確認依據，並保留 BFF 持久化與 theater handoff。Proof 使用 `pnpm visit:bff-qa` 的 deterministic server-owned VisitPlan，沒有呼叫 `/api/ai/visit` provider。
- 2026-06-20 resolved: BFF-202 已完成 Visit/report AI hardening；`/api/ai/visit` 與 `/api/ai/report` 皆維持 current-member scope、quota guard、success/error `AiUsageLog`，並新增 facts/inferences/unknowns/recommendations DTO boundary。`/api/ai/report` 保留 markdown 相容模式且新增 JSON DTO，CRM report subpage 已改用 JSON。Provider prompt 改用 provider-safe client snapshot，不送 email/phone/raw notes；`pnpm bff:visit-report-ai-qa` 通過 401/400/429/success/provider-error、VISIT/REPORT usage log 增量與 private sentinel 0。
- 2026-06-20 resolved: 本輪 scheduled fifth-loop whole-product gap review 已完成；cadence counter 重設為 0，下一輪 normal implementation/proof 建議 `PIM-010 Interview -> VisitPlan / Theater draft writeback`。原因：client/relationship/previsit/Route B interaction shell 已有 proof，但 AI 訪談目前只寫 CRM candidate/insight/follow-up，尚未直接建立 persisted 準備包或劇場草稿。Fallback 為 `BFF-203a SPIN source-truth hardening`，移除 legacy `/spin` mock outline/local seed proof gap。
- 2026-06-20 resolved: PIM-010 已完成 AI 訪談確認卡 -> 準備包草稿 / Route B 劇場草稿 writeback；高敏感缺 draft reason/riskAccepted 會 blocked，approved writeback 建立 persisted `VisitPlan` 與 DB-backed Route B session，保留 facts/inferences/unknowns/reasoning evidence，未知項轉 narrator questions，NPC <= 4，且不建立 confirmed CRM fact。`pnpm interview:draft-writeback-qa` 證明 no-provider、`AiUsageLog` unchanged、API/DB/browser proof；下一輪建議 `BFF-203a SPIN source-truth hardening`。
- 2026-06-20 resolved: BFF-203a 已完成 SPIN source-truth hardening；正式 `/spin` list/create/detail/outline 使用 member-scoped `spin_sessions` / `spin_messages` BFF，`/spin/[sessionId]` 不再呼叫 `/api/mock/ai/spin-outline`，Quickstart demo seed 保留但不作正式 proof。Server `PATCH` 只允許同階、往下一階或完成，保護 `SITUATION -> PROBLEM -> IMPLICATION -> NEED_PAYOFF`；outline 為 deterministic no-provider proof，不需 `AiUsageLog`。`pnpm spin:source-truth-qa`、Browser completed-session outline proof、`pnpm ai:bff-audit` 通過。下一輪建議 `BFF-103c CRM related-list/archive/update source-truth`，fallback `RAS-001 role-aware navigation contract / legacy SPIN visibility`。

## Production Approval

- 2026-06-22 update: relationship confirmation card state 的 preparation UI -> transient boundary bridge 已完成；剩餘的 refresh/new-context persistence 不是證據搜集問題，而是產品/資料模型決策。若要正式保存 advisor selection，需選擇 VisitPlan-owned JSON subdocument 或 dedicated table + migration/rollback，且 allowlist 只能包含 `cardId`、`state`、`updatedAt`、`sourceReferenceIds`、`safeNoteSummary`；不得保存 person label、relationship detail、confirmation prompt、raw transcript/provider payload 或 confirmed CRM fact。
- 2026-06-23 resolved/source update: LV3-REL-DECISION-001 已完成 source-backed persistence decision contract；`VisitRelationshipConfirmationStateBoundary.storageDecision` 現在列出 `visit-plan-json-subdocument` 與 `dedicated-relationship-confirmation-state-table` 兩個候選方案，並各自包含 allowlist、forbidden fields、migration note、rollback note、tradeoffs 與 self-runnable proof command。`selectedOption=null`、`decisionStatus=product_schema_decision_required`、`persistedToDatabase=false` 仍是正確 runtime 狀態；下一步仍需 operator/product schema 決定其中一種，才可做 DB persistence slice。
- 2026-06-22 resolved: scheduled fifth-loop whole-product review after AMM-005j completed; cadence counter reset to 0. Previous top gap (Route B red-line context downstream consumption by visit preparation and AI meeting notes) is now resolved, and `/notes` no longer adopts the local quick-note prototype as source of truth. Next normal loop should be source-backed `ITA-005k disabled/no-provider red-line compliance-review intake`: create an owner-scoped review-candidate contract from persisted Route B feedback/action context, but do not create formal legal/compliance findings, real notifications, provider calls, confirmed CRM fact writes, live detection claims, external registry publication, or raw private/provider payload storage without explicit approval.
- 2026-06-22 resolved: scheduled fifth-loop whole-product review after ITA-005f completed; cadence counter reset to 0. Top gap is now downstream consumption of persisted Route B red-line action context by visit preparation package or AI meeting notes. Next normal loop should be source-backed `ITA/AMM-005g red-line action context downstream consumption bridge`, preferably visit preparation first unless the AMM/notes dirty worktree is explicitly audited and adopted. Formal legal/compliance findings, real notifications, live detection, external registry publication, raw private/provider payload storage, and confirmed CRM fact writes remain out of scope without explicit approval.
- PIM-006 已改 Prisma schema 並對目前 `.env` development Supabase Postgres target 執行 additive `db push`；production schema migration 仍需明確 approval。
- ITA-003c 已對目前 `.env` development Supabase Postgres target 執行 Route B additive `db push` 並通過 persisted session proof；production schema migration / rollback plan 仍需明確 approval。
- AMM-002a 已對目前 `.env` development Supabase target 執行 additive `pnpm exec prisma db push`，使 `CLIENT_MEETING` 與 `InterviewMeetingSummary` 可進入 runtime proof；production migration / rollback plan 仍需明確 approval。
- 2026-06-21 approval update: live Realtime provider proof 與 Route B director/character/feedback provider calls 已獲 operator 批准可實作/驗證；仍需 provider env、usage/cost evidence，且 success/error path 都必須寫 `AiUsageLog`。Provider env 未設定時 UI/API 仍需維持 guarded-disabled，不得宣稱可正式互動。
- 2026-06-21 approval update: external NANDA / third-party registry publication、signing、public discovery endpoint 或 cross-org agent access **未批准**；只能維持 internal manifest / local adapter proof。
- 2026-06-21 approval update: production/payment/email/notification 可進行 code implementation；真實 env/provider console setup 記錄於 `manual-setting.md`，實際 real payment/email/notification enablement 需在 env 完成後以 QA proof 開啟。Production destructive DB、remote deletion、refund/void 仍需逐項明確 approval。
- BFF-103c 後的 production-facing source blockers：admin/pilot 頁仍直接讀 demo seed、notification reminder route 仍是 mock success，CRM policy/timeline/report/gap-analysis related-list DTO 仍未完全收斂。這些不得被用作正式 LV3 server-owned proof。Dashboard、Reports library/detail/share action、Issues BFF、Team/org aggregate BFF、Visit/pre-visit BFF、SPIN session BFF 與 CRM client lifecycle 已可作 server-owned proof；provider routes 仍需持續守住 AiUsageLog/usage audit。

## Operator 手動處理

- 2026-06-21 resolved: 先前記錄的 Next/Turbopack Google Font production build blocker 已以本輪 LCH-009 proof 收斂。已讀 `node_modules/next/dist/docs/01-app/01-getting-started/13-fonts.md`、`01-app/02-guides/production-checklist.md`、`01-app/03-api-reference/08-turbopack.md`，並重跑 `pnpm build` 完整通過；目前不需要 operator 針對 font path 做人工決策。若後續 build 再回歸失敗，再依 Next 16 bundled docs 處理 local/self-hosted font fallback。

## Session / Seed Data / Env / External Service

- 2026-06-21 resolved: BFF-103d related-list targeted proof 已在 DB 透過 IPv6/pg read 恢復後完整通過。`DEMO_QA_BASE_URL=http://localhost:3000 pnpm bff:crm-related-lists-qa` 覆蓋 unauth 401、member client/family/policy/visit/report create 201、related-lists 200、manager 403、policies/timeline/gap-analysis/reports desktop no overflow、gap-analysis mobile no overflow、DTO 合規欄位保留、raw report body / raw section fields / email / phone / policyNumber 不外洩、`AiUsageLog` count 147->147。原 2026-06-20 partial blocker 不再阻擋 BFF-103d。
- 2026-06-21 recovery status: 同一 Supabase DB direct host 仍無 IPv4 A record（`resolve4=ENODATA`），但有 IPv6 AAAA，且本輪從目前開發環境確認 `DATABASE_URL` pooler port 6543 與 `DIRECT_URL` direct port 5432 皆可 TCP/pg read `select 1`。DB-backed BFF/ITA proof 可恢復執行；若後續在 IPv4-only runtime 再失敗，需改用 Supabase pooler/IPv4-compatible connection string 或請 operator 更新 env。不得再把 LCH-009 font/build fallback 當下一輪替代。
- 2026-06-23 open update: scheduled whole-product review 以 `pnpm lv3:cross-flow-no-provider-qa` 發現 clean cross-flow runtime 仍會被 public status DB read 擋住：public home 經 `src/lib/public/status-repository.ts` 讀 `systemSettings` 時遇 Prisma `P1001 DatabaseNotReachable`，DNS probe 顯示 `resolve4 db.wwocdcicvpmbdmqvskzi.supabase.co = ENODATA`、`resolve6` 有 AAAA，且 dev browser 同時看到 `/api/bff/notifications` 404。這是 runtime/source degraded-mode gap 加上可選 env/DNS 修復，不是新的產品決策；下一輪建議 source-backed `BFF-305b public status degraded fallback + notification BFF alignment`。若 operator 也能提供 IPv4-compatible/pooler connection string，可縮小剩餘 live DB/browser proof 成本。
- 2026-06-23 resolved/source update: BFF-305b 已完成 source fallback，`pnpm public:status-degraded-qa` 以 invalid DB URL 驗證 public status/pricing/landing/pricing page 在 DB unavailable 下不再 500，且 `/api/bff/notifications` 回 disabled/no-delivery empty DTO，不再 404。剩餘若要跑完整 `pnpm lv3:cross-flow-no-provider-qa` 的 DB-backed 子 proof，仍可由 operator 提供 IPv4-compatible/pooler connection string 或在目前環境自行重跑；這不再阻擋 public degraded-mode source behavior。
- 2026-06-23 resolved/source update: LV3-CROSS-001 已完成 clean cross-flow no-provider proof repair；Route B public session create/read/turn/append responses 透過 `toPublicRouteBSessionSnapshot` 移除 internal `scene.redLineActionState` persistence envelope，避免 public response/page 被 private/provider sentinel 誤判，同時 feedback review 仍可 owner-scoped 消費內部 scene state。`pnpm lv3:cross-flow-no-provider-qa` 通過 7 段 proof，`AiUsageLog` 180->180。
- 2026-06-23 resolved/source update: BFF-402a 已移除 `/api/notifications/visit-reminder` mock email success；route 現在採 shared validation、current-member auth、503 disabled/no-delivery DTO，且 DB auth 不可達時 fail closed 為 `VISIT_REMINDER_AUTH_UNAVAILABLE`、`providerAttempted=false`。`pnpm notification:visit-reminder-disabled-qa` 在目前 Supabase DB `P1001 DatabaseNotReachable` 下通過 source、401/400、DB-unavailable 503、private sentinel 0；DB 可達後 operator 可自行重跑同一 command 驗 authenticated disabled DTO。真 notification/email provider、job queue、sender policy、idempotency 與 production env 仍是 external service / manual setting blocker，不得宣稱 real delivery ready。
- 2026-06-23 resolved/source update: BFF-402b 已新增 ECPay notify/query fail-closed skeleton：`POST /api/billing/ecpay/notify` 支援 provider-shaped JSON/form payload 但只回 503 disabled，`POST /api/billing/ecpay/query` 先做 server-owned authenticated disabled/query-unavailable boundary。`pnpm billing:ecpay-disabled-qa` 通過 57/57 checks，覆蓋 notify 400/503、duplicate notify duplicate-safe/no-ledger/no-transaction/no-activation、query 400/401、目前 DB `P1001 DatabaseNotReachable` 下 query auth fail-closed 503、payment/private sentinel 0。真 CheckMacValue 驗證、ECPay server query、transaction ledger persistence、refund/void/manual review、production payment enablement 與 callback domain/env 仍是 manual/provider/DB proof blocker。
- 2026-06-23 resolved/source update: BFF-403a 已新增 subscription capability read BFF：`GET /api/billing/subscription` 回 versioned `BillingSubscriptionCapabilityDto`，包含 current plan/status、server capability、quota、seat/collaborator/unit usage、checkout disabled status、confirmed transaction/query-only activation boundary，並接入 `/api/workspace/bootstrap` 的 `subscription` contract。`BILLING_SUBSCRIPTION_QA_BASE_URL=http://127.0.0.1:3058 pnpm billing:subscription-capability-qa` 通過 source/API guarded proof；目前 DB `P1001 DatabaseNotReachable` 下 authenticated request fail-closed 503 `BILLING_SUBSCRIPTION_UNAVAILABLE` 且 `providerAttempted=false`、no-store/request-id、payment/private sentinel 0、repository 無 billing write/provider call/fake `AiUsageLog`。DB 可達後 operator 可自行重跑同一 command 檢查 authenticated 200 + bootstrap payload；剩餘 BFF-403 blockers 是 org/member UI consumption、real plan-change persistence、confirmed transaction/query activation proof、ledger idempotency 與 production payment env/callback，不應再以 docs-only evidence 補。
- 2026-06-23 resolved/source update: BFF-403b 已完成 subscription capability UI consumption；dashboard layout 會 server-side 讀 `BillingSubscriptionCapabilityDto` 並注入 member/orgAdmin sidebar render model，workspace bootstrap navigation 同步消費 `subscription`，RoleAwareSidebar 顯示 plan/AI/seat/collaborator/unit/checkout disabled boundary，且 render model 明確標示 `server_subscription_dto` 或 `session_plan_capability_fallback`。`pnpm billing:subscription-ui-qa`、`pnpm nav:sidebar-ui-qa`、`pnpm nav:sidebar-renderer-contract-qa` 通過；無 provider/payment call、無 billing write、無 fake `AiUsageLog`、無 browser/localStorage plan assumption。剩餘 BFF-403 blockers 縮小為 real plan-change persistence、confirmed transaction/query activation proof、ledger idempotency 與 production payment env/callback。
- 2026-06-23 resolved/source update: BFF-402c/BFF-403c 已完成 plan-change activation/idempotency guarded boundary；新增 `POST /api/billing/plan-change` 與 versioned `BillingPlanChangeDisabledDto`，並在 `BillingSubscriptionCapabilityDto.planChangeStatus` 暴露 `/api/billing/plan-change` endpoint。route 只允許 server-validated self-serve target plan，current-member scoped，DB auth 不可達時 fail closed 為 `BILLING_PLAN_CHANGE_AUTH_UNAVAILABLE`、`providerAttempted=false`；成功進入 route 也只回 503 `BILLING_PLAN_CHANGE_DISABLED`，明確宣告 no provider call、no order/transaction/ledger write、no organization plan update、no redirect/browser-only activation、no raw provider/payment payload、no fake `AiUsageLog`。`pnpm billing:plan-change-boundary-qa` 通過 source/static proof；若 dev server/session 可用，operator 可自行跑 `BILLING_PLAN_CHANGE_QA_BASE_URL=http://127.0.0.1:<port> pnpm billing:plan-change-boundary-qa --api` 檢查 runtime 400/401/503 response。剩餘付款 blockers 是真 CheckMacValue 驗證、ECPay server query、transaction ledger persistence/idempotent upsert、confirmed transaction/query activation、production payment env/callback，以及 refund/void/destructive payment actions 的逐項 approval。
- 2026-06-23 resolved/source update: BFF-402d 已完成 transaction ledger idempotency source contract；新增 shared `asai.billing.ledger_idempotency.v1`，定義 `organizationId + provider + merchantTradeNo` 唯一 scope、server-owned upsert target、duplicate notification 不重複啟用、accepted activation statuses `PAID`/`QUERY_CONFIRMED`、no raw provider/checksum storage、no provider call/no DB write/no fake `AiUsageLog`。ECPay notify/query disabled DTO 與 plan-change disabled DTO 皆引用此 contract；`pnpm billing:ledger-idempotency-qa`、`pnpm billing:plan-change-boundary-qa`、`BILLING_ECPAY_DISABLED_QA_BASE_URL=http://127.0.0.1:3061 pnpm billing:ecpay-disabled-qa` 通過（ECPay runtime 73/73）。此 proof 仍不代表真正 `PaymentTransaction` persistence/upsert、CheckMacValue 驗證、ECPay server query confirmation、confirmed transaction/query activation 或 refund/void/manual review 已完成；DB 可達時 operator 可重跑同一 ECPay QA command 補 authenticated query disabled DTO runtime evidence。
- 2026-06-23 resolved/source update: BFF-402e 已完成 server-only CheckMacValue validation boundary；新增 `asai.billing.ecpay.checkmac.v1`、server-side SHA256 canonicalization/verification helper、notify route server-only `ECPAY_HASH_KEY` / `ECPAY_HASH_IV` injection，且 DTO 只回驗證狀態，不回 raw CheckMacValue、HashKey、HashIV 或 provider payload。`pnpm billing:ecpay-checkmac-qa` 通過 static + deterministic fixture；`BILLING_ECPAY_DISABLED_QA_BASE_URL=http://127.0.0.1:3062 pnpm billing:ecpay-disabled-qa` 通過 99/99，覆蓋 valid checksum verified、duplicate valid notify duplicate-safe/no-write/no-activation、tampered checksum invalid、payment/private sentinel 0；`pnpm billing:ledger-idempotency-qa` 仍通過。剩餘 payment blockers 已縮小為 ECPay server query confirmation、真正 `PaymentTransaction` persistence/upsert、confirmed transaction/query activation、production payment env/callback，以及 refund/void/destructive payment actions 的逐項 approval。
- 2026-06-23 resolved/source update: BFF-402f 已完成 ECPay server query guarded boundary；`/api/billing/ecpay/query` 的 disabled DTO 現在包含 `asai.billing.ecpay.server_query_boundary.v1`，明確 server-owned endpoint、browser query disabled、client-supplied organization/amount untrusted、providerAttempted=false、confirmationReceived=false、PaymentTransaction upsert not attempted、organization plan not updated、no fake `AiUsageLog`，並引用 ledger idempotency contract。`pnpm billing:ecpay-query-boundary-qa`、`BILLING_ECPAY_DISABLED_QA_BASE_URL=http://127.0.0.1:3063 pnpm billing:ecpay-disabled-qa`、`pnpm bff:release-readiness-qa` 通過。此結果不代表 real ECPay provider query response validation、PaymentTransaction persistence/upsert、confirmed activation、production payment env/callback、refund/void/manual review 或 production payment enablement 已完成；release readiness 只把 `ecpay_server_query_guarded_boundary` 標為 proven，`ecpay_server_query_confirmation` 仍 blocked/provider_env。
- 2026-06-23 resolved/source update: BFF-402g 已完成 PaymentTransaction persistence guarded contract；ECPay notify/query disabled DTO 現在包含 `asai.billing.payment_transaction_persistence.v1`，明確 `organizationId + provider + merchantTradeNo` unique scope、verified statuses `PAID`/`QUERY_CONFIRMED`、notify 需 CheckMacValue、query 需 server query confirmation、allowlisted provider summary only、no DB upsert、no subscription order update、no organization plan activation、no raw provider payload/checksum/private data storage、no provider call、no fake `AiUsageLog`。`pnpm billing:payment-transaction-persistence-qa`、`BILLING_ECPAY_DISABLED_QA_BASE_URL=http://127.0.0.1:3064 pnpm billing:ecpay-disabled-qa`、`pnpm billing:ledger-idempotency-qa`、`pnpm bff:release-readiness-qa` 通過。此結果不代表 real `PaymentTransaction` DB upsert/write、real ECPay query confirmation、confirmed activation、production payment env/callback、refund/void/manual review 或 production payment enablement 已完成；release readiness 只把 `payment_transaction_persistence_guarded_contract` 標為 proven，`payment_transaction_persistence` 仍 blocked/db。
- 2026-06-23 resolved/source update: BFF-402h 已完成 confirmed activation guarded contract；ECPay notify/query disabled DTO 現在包含 `asai.billing.confirmed_activation.v1`，明確 `Organization.plan` activation target、confirmed ledger + PaymentTransaction persistence/write prerequisites、accepted statuses `PAID`/`QUERY_CONFIRMED`、notify 需 checksum validation、query 需 server query confirmation，並拒絕 redirect return URL、browser payment result、client-supplied plan/amount/organization、localStorage plan 作為啟用依據。`pnpm billing:confirmed-activation-qa`、`BILLING_ECPAY_DISABLED_QA_BASE_URL=http://127.0.0.1:3065 pnpm billing:ecpay-disabled-qa`、`pnpm billing:payment-transaction-persistence-qa`、`pnpm billing:ledger-idempotency-qa`、`pnpm bff:release-readiness-qa` 通過。此結果不代表 real `PaymentTransaction` DB upsert/write、real ECPay provider query confirmation、live `Organization.plan` mutation、production payment env/callback、refund/void/manual review 或 production payment enablement 已完成；release readiness 只把 `confirmed_activation_guarded_contract` 標為 proven，`confirmed_activation` 仍 blocked/source。
- PIM-006 已 resolved；本輪 proof 使用 `ALLOW_DEV_AUTH_HEADER=true` 的 local dev server 與 demo member/manager header。
- PIM-008 已 resolved；browser writeback proof 使用 demo member header 與自動建立 demo client 完成。
- PIM-009 已 resolved；cross-mode QA 使用 local dev server、demo member/manager header、development Supabase DB proof 與 headless browser desktop/mobile proof 完成。
- 後續若要做真實 browser WebRTC/voice transport proof，需要可用 OpenAI Realtime model、前端 WebRTC client、麥克風 permission 測試條件與 usage/cost evidence。
- Production demo login runtime DB 已於 2026-06-19 resolved：Vercel Production `DATABASE_URL` 改為 Supabase shared transaction pooler，`DIRECT_URL` 改為 shared session pooler，redeploy `3DbtCFYPqQ99VcFvGEvrkbpoqzrB` 後正式 `/api/public/pricing` 200、`/api/share/demo-share-wang` 200、demo member one-click login 進 `/dashboard`。

## 已解決 / 不再阻擋

- 2026-06-23 resolved/source update: LV3-THEATER-MEETING-SIGNAL-STAGE-SESSION-001 已完成 Route B session source metadata propagation；`/theater/build` 會把 meeting-derived relationship signal stage cards 轉成 safe `sourceGrounding.meetingRelationshipSignals`，Route B session draft 會寫入 `sceneState` 與 metadata，BFF readback 會回到 `RouteBSessionSnapshot.scene.sourceGrounding`，`/theater/[sessionId]` 以 `data-route-b-meeting-signal-source-grounding` 顯示 grounded card / unknown / narrator question preview。`pnpm theater:meeting-signal-session-source-qa`、`pnpm theater:route-b-schema-dry-run`、`pnpm ai:protocol-registry-qa`、`pnpm ai:bff-audit`、`pnpm exec tsc --noEmit --pretty false`、`pnpm lint:changed` 通過；proof 明確 no browser-supplied session/person id、no raw transcript/provider/contact/policy leakage、no provider/no fake `AiUsageLog`、no relationship graph/VisitPlan/CRM fact write。若只剩 live browser/API/DB evidence，operator 可在 dev server/DB 可用時自行跑 `DEMO_QA_BASE_URL=http://localhost:3000 pnpm theater:route-b-persistence-qa`；此結果仍不代表 relationship confirmation card advisor-state 已 refresh/new-context persistence。
- 2026-06-23 resolved/source update: LV3-THEATER-MEETING-SIGNAL-STAGE-001 已完成 source/UI stage review；`/theater/build` 的準備包來源審查現在會從 safe `knownMaterials` 解析 meeting-derived relationship signal cards，顯示 FACT/INFERENCE/UNKNOWN chips、source/action/priority labels、摘要與旁白補問 preview，並明示 Stage-only：不寫回關係圖、VisitPlan 或 CRM 事實。`pnpm visit:meeting-signal-theater-handoff-qa` 已升級驗 stage card / narrator preview / no-write copy；`pnpm visit:theater-handoff-dry-run` 仍證明 meetingRelationshipSignals no-provider/no-DB/no-write summary。這不代表 relationship confirmation card state 已持久化，也不代表 Route B persisted session 已保存 meeting-signal source metadata；下一輪若要推進，應做 session creation/source metadata propagation。
- 2026-06-23 resolved/source update: LV3-MEETING-SIGNAL-THEATER-001 已完成 source-backed bridge；`VisitMeetingRelationshipSignalDeck` 現在可由 owner-scoped `/api/visits/[id]/theater-handoff` lookup 帶入 `buildVisitTheaterHandoff()`，輸出 safe `knownMaterials`、`sourceSummary.evidenceSummary.meetingRelationshipSignals`、meeting signal source count 與 unknown/ASK_IN_NEXT_VISIT narrator questions。`/pre-visit/[planId]` 的本地 preview 與 `/theater/build` source review 也會消費同一 deck/count。`pnpm visit:theater-handoff-dry-run`、`pnpm visit:meeting-signal-theater-handoff-qa`、`pnpm visit:meeting-relationship-signal-dry-run`、`pnpm visit:meeting-relationship-signal-bff-ui-qa`、`pnpm ai:protocol-registry-qa`、`pnpm exec tsc --noEmit --pretty false`、`pnpm lint:changed` 通過；proof 明確 no browser-supplied meeting session/person id、no provider/no fake AiUsageLog、no raw transcript/provider/contact/policy/secret leakage、no relationship graph/VisitPlan/CRM fact write。這不代表 relationship confirmation card state 已持久化，也不代表 external NANDA registry publication 已核可。
- 2026-06-19 resolved: PIM-001 已補上顧問陪談與劇場場域建構共用的 memory domain contract、retrieval scoring 與 correction/supersede helper；PIM-002 可直接接 API route。
- 2026-06-19 resolved: PIM-002 已讓 `/api/ai/interview` 與 `/api/ai/interview/outputs` 使用 request-local memory stream、retrieval partition、micro-plan 與 supporting memory IDs；PIM-003 可沿用同樣模式接劇場場域建構。
- 2026-06-19 resolved: PIM-003 已新增 Theater field build outline、Park memory reflection 與 `TheaterBuildPacket` builder；fixture proof 已確認 NPC <= 4、資料不足回補問、推論不會寫入 confirmed facts。PIM-004 可接中文語音 UX shell。
- 2026-06-19 resolved: PIM-004 已完成 `/interview` 中文語音 UX shell、mic consent、live stage、transcript correction placeholder、memory rail、文字 fallback 與中文 IME composition guard；PIM-005 可接 Realtime session BFF。
- 2026-06-19 resolved: PIM-005 已新增 authenticated Realtime session BFF、quota guard、short-lived client secret production path、non-production dry-run proof、event mirror allowlist、raw-audio/secret reject、final transcript memory extraction 與 usage/session marker；PIM-006 可接 Prisma persistence。
- 2026-06-19 resolved: PIM-006 已新增 `InterviewSession` / `InterviewTurn` / `InterviewMemory` / `InterviewReflection` schema、owner-scoped repository / BFF routes、development db push 與 `pnpm interview:persistence-qa`；proof 確認清空 browser storage 等價的 stateless API read 可恢復 session/memory/reflection，manager 不能讀 member 私有逐字稿/記憶。PIM-007 可接 reflection/planning service。
- 2026-06-19 resolved: PIM-007 已新增 deterministic reflection/planning service、generated reflection BFF、plan BFF 與 `pnpm interview:reflection-planning-qa`；proof 確認 confirmed facts / inferred patterns / unknowns 分流、supporting memory IDs 保留、plan 優先追問 unknown、不重問 confirmed fact，且 manager 不能從 member 私有記憶生成 plan。PIM-008 可接 confirmation card / CRM writeback boundary。
- 2026-06-19 resolved: PIM-008 已新增 confirmation card / CRM writeback boundary、`GET/POST /api/ai/interview/sessions/[sessionId]/writebacks`、`pnpm interview:writeback-qa` 與 `pnpm interview:writeback-browser-qa`；proof 確認 confirmed fact + checked + 高敏感 approval 才建立 CRM candidate interaction event，inference checked 只保存為 interview insight 且 DB metadata 證明 inference CRM fact = 0，unknown 轉 follow-up task。PIM-009 可接 cross-mode QA 與 rollback notes。
- 2026-06-19 resolved: PIM-009 已新增 `pnpm interview:cross-mode-qa`，串接 advisor/theater/voice/persistence/reflection/writeback/browser/privacy proof；並在 `PLN-018` 補 voice provider disabled、memory persistence disabled、schema rollback、CRM writeback rollback 與 manager aggregate privacy rollback note。PIM workstream 第一階段可交接；剩餘為 live Realtime provider proof、raw audio retention approval 與 production migration approval。
- 2026-06-19 resolved: Production demo login blocker 已確認為 Vercel 使用 Supabase direct/dedicated DB host 造成 `P1001 DatabaseNotReachable`；已切到 shared IPv4 pooler 並 redeploy，正式 demo member 登入 proof 通過。
